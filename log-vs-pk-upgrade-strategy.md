# 日志表与主键表的差异化升级策略

Fluss 从 data lake 接管已有表时，日志表与主键表的升级路径应区别设计。

## 1. 日志表升级策略

日志表的语义相对简单，核心特点是：

- 以追加写为主
- 不需要主键覆盖
- 不需要旧值查找
- 不需要基于旧值生成 changelog

因此，日志表的升级重点是**分区接管**，而不是**状态接管**。

### 处理方式

对于日志表，系统通常只需要：

- 创建对应分区
- 创建 bucket
- 分配副本
- 通知 Tablet Server 持有副本
- 按现有流程对外提供服务

### 设计结论

日志表不需要额外构建用于主键反查的基线状态，也不需要在 Fluss 内部先恢复主键视图。  
因此，日志表升级可以直接复用 Fluss 现有的分区创建与副本管理流程，整体实现相对简单。

## 2. 主键表升级策略

主键表的语义更复杂，核心特点是：

- 需要维护主键唯一性
- 需要处理更新覆盖
- 需要处理删除语义
- 写入时需要查找已有值
- 需要基于旧值生成 changelog

因此，主键表的升级重点不是简单地“把分区接进来”，而是**让 Fluss 接管热分区的状态与写入**。

### 处理方式

对于主键表，凡是后续还要继续承载写入的热分区，必须先完成以下准备：

- 将 data lake 中对应热分区转换为 Fluss 可加载的基线 SST
- 让 Tablet Server 持有这份 Imported Base
- 在本地形成可用于主键反查的 base state
- 在此基础上再进入 Fluss 现有分区与写入流程

只有这样，Fluss 才能在写入时正确完成：

- 主键查找
- 旧值读取
- changelog 生成
- 更新和删除覆盖

### 设计结论

对于主键表，Fluss 必须真正承载热分区写入。  
如果没有先准备好可反查的基线状态，就无法保证主键语义和 changelog 生成的正确性。

## 3. 差异总结

| 类型 | 升级重点 | 是否需要基线状态 | 是否需要旧值反查 | 是否需要 Fluss 承载热分区写入 |
|---|---|---|---|---|
| 日志表 | 分区接管 | 否 | 否 | 通常不需要额外处理 |
| 主键表 | 状态接管 + 写入接管 | 是 | 是 | 是 |

## 4. 总体结论

- **日志表升级**本质上是分区接管问题，可直接复用现有分区和副本流程。
- **主键表升级**本质上是热分区状态接管问题，必须先准备 Imported Base，再由 Fluss 承载热分区写入。
